* Where is the public key?
* finish the implementations
* master the rust codes
* Make sure that changing the signature is consistent, otherwise the idea is to fake an inconsistent private key
* Testing?
